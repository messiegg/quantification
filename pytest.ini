[pytest]
testpaths = tests
markers =
    data_required: requires local market data files
    full_backtest: runs full backtest and may take longer
    release_guard: release guard checks
    observation: observation pipeline tests
    no_network: must not access network
